{% extends 'JDRMainBundle::layout.html.twig' %}

{% block core %}
	<div class="row">
	<h1>Page de connexion</h1>
	<hr>
	{% if error %}
    	<div class="alert alert-danger">{{ error.message }}</div>
	{% endif %}
		<form action="{{ path('login_check') }}" method="post" class="col-md-6 col-md-offset-3">
			<div class="form-group">
				<input class="form-control" type="text" name="_email" placeholder="Email" value="{{ last_email }}">
			</div>
			<div class="form-group">
				<input class="form-control" type="password" name="_password" placeholder="Password">
			</div>
			<div class="form-group">
				<button class="form-control btn btn-primary" type="submit">Connexion</button>
			</div>
		</form>
	</div>
{% endblock %}
